import styled from "styled-components";

export const DivInfo = styled.div`
    display: flex;
    align-items: center;
    justify-content: start;

    padding: .25rem .625rem;

    border-radius: .5rem;
    box-shadow: 0rem 0rem .313rem rgba(0, 0, 0, 0.2);
    gap: .625rem;
`;